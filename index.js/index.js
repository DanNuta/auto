"use strict";
class CreateAuto {
    constructor(id, model, anul, pret, km, title) {
        this.id = id,
            this.model = model,
            this.anul = anul,
            this.pret = pret,
            this.km = km,
            this.title = title;
    }
}
class AddAutoList {
}
const auto = new AddAutoList();
console.log(auto);
